[project]
name = "arborinth"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Erwin Janssen", email = "code@erwinjanssen.nl" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
arborinth = "arborinth:main"

[build-system]
requires = ["uv_build>=0.9.30,<0.10.0"]
build-backend = "uv_build"
